/**
 * Work experience constants.
 * @module src/domain/constants/workExperience.constant
 */

/**
 * Contains numeric constants used in the validation of IWorkExperience objects.
 *
 * @type {object}
 * @property {number} JOB_TITLE_MIN_LENGTH - The minimum length accepted for a job title.
 */
export const workExperienceConstants = {
  /**
   * The minimum length accepted for a job title.
   * @type {number}
   */
  JOB_TITLE_MIN_LENGTH: 3,
};
